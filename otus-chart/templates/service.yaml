apiVersion: v1
kind: Service
metadata:
  name: {{ include "otus_chart.fullname" . }}
  labels:
    {{- include "otus_chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "otus_chart.selectorLabels" . | nindent 4 }}

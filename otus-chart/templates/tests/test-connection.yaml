apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "otus_chart.fullname" . }}-test-connection"
  labels:
    {{- include "otus_chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "otus_chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
